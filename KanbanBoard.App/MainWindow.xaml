<Window x:Class="KanbanBoard.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KanbanBoard.App"
        xmlns:icons="clr-namespace:KanbanBoard.App.Icons"
        xmlns:models="clr-namespace:KanbanBoard.Core.Models;assembly=KanbanBoard.Core"
        mc:Ignorable="d"
        Title="KanbanBoard" Height="600" Width="1000">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <DataTemplate x:Key="CardTemplate">
            <Border x:Name="ItemBorder"
                    Background="White"
                    BorderBrush="LightGray"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="8"
                    Margin="4">
                <StackPanel>
                    <TextBlock Text="{Binding Title}"
                               FontWeight="Bold"
                               FontSize="13"
                               TextWrapping="Wrap"                       
                               MaxHeight="40"                       
                               HorizontalAlignment="Center"                      
                               Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding Description}"                       
                               TextWrapping="Wrap"                       
                               MaxHeight="40"                       
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding DueDate, StringFormat='Fällig: {0:dd.MM.yyyy}'}"                       
                               FontSize="11"                       
                               Foreground="Gray"/>
                </StackPanel>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="{x:Static models:CardStatus.ToDo}">
                    <Setter TargetName="ItemBorder" Property="Background" Value="LightPink"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="{x:Static models:CardStatus.Doing}">
                    <Setter TargetName="ItemBorder" Property="Background" Value="LightSkyBlue"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="{x:Static models:CardStatus.Done}">
                    <Setter TargetName="ItemBorder" Property="Background" Value="LightSeaGreen"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>

    <Grid x:Name="RootGrid"
          Background="{Binding Theme.WindowBackground}"
          AllowDrop="True"
          DragOver="RootGrid_DragOver">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menu-Bar -->
        <Border Grid.Row="0"
                Background="{Binding Theme.ToolbarBackground}"
                BorderBrush="{Binding Theme.ToolbarBorder}"
                BorderThickness="0,0,0,2">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,5,0,5">

                <Button Width="32" Height="32"
                        BorderThickness="0"
                        Background="Transparent"
                        FocusVisualStyle="{x:Null}"
                        Command="{Binding CreateCardCommand}"
                        ToolTip="add new card"
                        Margin="0,0,20,0">
                    <Viewbox Width="28" Height="28">
                        <Grid Width="16" Height="16">
                            <Grid.Resources>
                                <Style TargetType="Path">
                                    <Setter Property="Fill"
                                            Value="{Binding Theme.TextColor}"/>
                                </Style>
                            </Grid.Resources>
                            <Path Data="{x:Static icons:AppIcons.PlusSquare}"/>
                        </Grid>
                    </Viewbox>
                </Button>

                <Button Width="32" Height="32"
                        BorderThickness="0"
                        Background="Transparent"
                        FocusVisualStyle="{x:Null}"
                        Command="{Binding DeleteCardCommand}"
                        ToolTip="delete selected card"
                        Margin="0,0,20,0">
                    <Viewbox Width="28" Height="28">
                        <Grid Width="16" Height="16">
                            <Grid.Resources>
                                <Style TargetType="Path">
                                    <Setter Property="Fill"
                                            Value="{Binding Theme.TextColor}"/>
                                </Style>
                            </Grid.Resources>
                            <Path Data="{x:Static icons:AppIcons.Trash}"/>
                        </Grid>
                    </Viewbox>
                </Button>

                <ComboBox Width="100"
                          Height="28"
                          FontSize="14"
                          FontWeight="Bold"
                          BorderThickness="0"
                          ItemsSource="{Binding SortModes}"
                          SelectedItem="{Binding SelectedSortMode}"
                          ToolTip="sort cards">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="FontWeight" Value="Normal"/>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
                <Button Width="16" Height="28"
                        BorderThickness="0"
                        FocusVisualStyle="{x:Null}"
                        Command="{Binding ChangeSortDirectionCommand}"
                        ToolTip="ascending / descending">
                    <Viewbox Width="16" Height="16"
                             HorizontalAlignment="Center">
                        <Grid Width="16" Height="16">
                            <Grid.Resources>
                                <Style TargetType="Path">
                                    <Setter Property="Fill"
                                            Value="{Binding Theme.TextColor}"/>
                                </Style>
                            </Grid.Resources>
                            <Path Data="{Binding SortDirectionIcon}"/>
                        </Grid>
                    </Viewbox>
                </Button>
            </StackPanel>
        </Border>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,5,30,5">
            <Button Width="32" Height="32"
                    BorderThickness="0"
                    Background="Transparent"
                    FocusVisualStyle="{x:Null}"
                    Command="{Binding ChangeDarkmodeCommand}"
                    ToolTip="Darkmode / Whitemode">
                <Viewbox Width="20" Height="20">
                    <Grid Width="16" Height="16">
                        <Grid.Resources>
                            <Style TargetType="Path">
                                <Setter Property="Fill"
                                        Value="{Binding Theme.TextColor}"/>
                            </Style>
                        </Grid.Resources>
                        <Path Data="{Binding Theme.DarkmodeIcon}"/>
                    </Grid>
                </Viewbox>
            </Button>
        </StackPanel>

        <!-- Card-Lists: ToDo / Doing / Done -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ToDo -->
            <TextBlock Grid.Column="0" Grid.Row="0"
                       Text="To Do"
                       FontWeight="Bold"
                       FontSize="20"
                       Foreground="{Binding Theme.TextColor}"
                       VerticalAlignment="Bottom"
                       HorizontalAlignment="Center"
                       Margin="10,10,10,0"/>
            <ListBox Grid.Column="0" Grid.Row="1"
                     Background="{Binding Theme.ColumnBackground}"
                     BorderBrush="{Binding Theme.ColumnBorder}"
                     BorderThickness="2"
                     ItemsSource="{Binding ToDoCards}"
                     SelectedItem="{Binding SelectedCard}"
                     ItemTemplate="{StaticResource CardTemplate}"
                     Margin="10"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     PreviewMouseMove="ListBox_PreviewMouseMove"
                     AllowDrop="True"
                     Drop="ListBox_Drop"
                     MouseDoubleClick="ListBox_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <!-- Doing -->
            <TextBlock Grid.Column="1" Grid.Row="0"
                       Text="Doing"
                       FontWeight="Bold"
                       FontSize="20"
                       Foreground="{Binding Theme.TextColor}"
                       VerticalAlignment="Bottom"
                       HorizontalAlignment="Center"
                       Margin="10,10,10,0"/>
            <ListBox Grid.Column="1" Grid.Row="1"
                     Background="{Binding Theme.ColumnBackground}"
                     BorderBrush="{Binding Theme.ColumnBorder}"
                     BorderThickness="2"
                     ItemsSource="{Binding DoingCards}"
                     SelectedItem="{Binding SelectedCard}"
                     ItemTemplate="{StaticResource CardTemplate}"
                     Margin="10"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     PreviewMouseMove="ListBox_PreviewMouseMove"
                     AllowDrop="True"
                     Drop="ListBox_Drop"
                     MouseDoubleClick="ListBox_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <!-- Done -->
            <TextBlock Grid.Column="2" Grid.Row="0"
                       Text="Done"
                       FontWeight="Bold"
                       FontSize="20"
                       Foreground="{Binding Theme.TextColor}"
                       VerticalAlignment="Bottom"
                       HorizontalAlignment="Center"
                       Margin="10,10,10,0"/>
            <ListBox Grid.Column="2" Grid.Row="1"
                     Background="{Binding Theme.ColumnBackground}"
                     BorderBrush="{Binding Theme.ColumnBorder}"
                     BorderThickness="2"
                     ItemsSource="{Binding DoneCards}"
                     SelectedItem="{Binding SelectedCard}"
                     ItemTemplate="{StaticResource CardTemplate}"
                     Margin="10"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     PreviewMouseMove="ListBox_PreviewMouseMove"
                     AllowDrop="True"
                     Drop="ListBox_Drop"
                     MouseDoubleClick="ListBox_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Grid>
    </Grid>
</Window>